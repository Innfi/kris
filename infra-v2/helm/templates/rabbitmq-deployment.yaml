apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namesapce: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.rabbitmq.replicaCount }}
  selector:
    matchLabels:
      name: rabbitmq
  template:
    metadata:
      labels:
        name: rabbitmq
    spec:
      containers:
      - name: mq_single
        image: {{ .Values.rabbitmq.image }}
        resources:
          requests:
            cpu: 200m
            memory: 512Mi
        ports:
        - containerPort: 5672
